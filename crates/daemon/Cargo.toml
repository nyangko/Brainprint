[package]
name = "brainprint-daemon"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "brainprintd"
path = "src/main.rs"

[dependencies]
brainprint-core = { path = "../core" }
brainprint-engine = { path = "../engine" }
