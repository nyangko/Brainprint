// A project-wide import. `RunnerTests.cs` names `Runner` and
// `Overloads` without a file-level `using Core;`, so a binding that
// only understands file-level imports cannot resolve them.
global using Core;
