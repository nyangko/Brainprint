// The *same* namespace as Program.cs declares with file-scoped syntax.
// Two syntaxes, one namespace identity.
namespace App
{
    public sealed class Service
    {
        public void Run() { }
    }
}
