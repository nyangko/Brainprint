namespace Contracts.Other;

// The same simple name as `Contracts.Model`, in another namespace. An
// alias that resolves by name rather than by binding lands here.
public sealed class Model
{
    public string Name { get; set; } = "";
}
